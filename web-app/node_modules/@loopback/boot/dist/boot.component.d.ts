import { Application, Component } from '@loopback/core';
import { ApplicationMetadataBooter, ControllerBooter, DataSourceBooter, RepositoryBooter, ServiceBooter, LifeCycleObserverBooter } from './booters';
/**
 * BootComponent is used to export the default list of Booter's made
 * available by this module as well as bind the BootStrapper to the app so it
 * can be used to run the Booters.
 */
export declare class BootComponent implements Component {
    booters: (typeof ControllerBooter | typeof DataSourceBooter | typeof RepositoryBooter | typeof ServiceBooter | typeof ApplicationMetadataBooter | typeof LifeCycleObserverBooter)[];
    /**
     *
     * @param app - Application instance
     */
    constructor(app: Application);
}
